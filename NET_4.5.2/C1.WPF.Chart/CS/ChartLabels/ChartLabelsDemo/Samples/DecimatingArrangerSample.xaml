<UserControl x:Class="LabelSamples.DecimatingArrangerSample"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             xmlns:c1="http://schemas.componentone.com/winfx/2006/xaml"
             >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <Button x:Name="btnNew" Content="New data" Click="btnNew_Click" />
            <CheckBox x:Name="cbDecimation" Margin="8,2" VerticalAlignment="Center" Content="Enable label decimation" />
        </StackPanel>

        <c1:C1Chart x:Name="chart" ChartType="XYPlot" Grid.Row="1" />
    </Grid>
</UserControl>
