<Window x:Class="DiagramExplorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DiagramExplorer"
        mc:Ignorable="d"
        Title="MainWindow" Height="640" Width="960" Background="{DynamicResource BackgroundColor2}"
        DataContext="{Binding RelativeSource={RelativeSource Mode=Self}}">
    
    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}" />
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}" />
        </Style>
        <Style TargetType="Border">
            <Setter Property="Background" Value="{DynamicResource BackgroundColor}" />
        </Style>

        <Style TargetType="TreeView">
            <Setter Property="Background" Value="{DynamicResource BackgroundColor}" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}" />
        </Style>

        <Style TargetType="TreeViewItem">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}" />
        </Style>

    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Border Padding="10" BorderThickness="0,0,1,0" >
            <TreeView x:Name="treeView" Padding="5" BorderThickness="0" ItemsSource="{Binding AllItems}"
                      FontSize="14" SelectedItemChanged="treeView_SelectedItemChanged">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                        <TextBlock Text="{Binding Name}" Foreground="{DynamicResource ForegroundColor}" />
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="Padding" Value="8" />
                        <Setter Property="IsExpanded" Value="True" />
                    </Style>
                </TreeView.ItemContainerStyle>
            </TreeView>
        </Border>

        <Grid x:Name="samplePage" Grid.Column="1" Margin="8" >

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Padding="10" CornerRadius="5" Margin="0,0,0,8">
                <StackPanel Orientation="Vertical">
                    <Label FontSize="18" FontWeight="DemiBold" Content="{Binding Title}" />
                    <TextBlock FontSize="14" TextWrapping="WrapWithOverflow" Text="{Binding Description}" />
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Padding="10" CornerRadius="5" Margin="0,0,0,8">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Orientation="Horizontal">
                        <Label Content="_Direction" />
                        <ComboBox x:Name="comboBoxDirection" SelectedIndex="0" />

                        <Label Content="_EdgeRouting" />
                        <ComboBox x:Name="comboBoxEdgeRouting" SelectedIndex="0"/>

                        <Label x:Name="labelPalette" Visibility="{Binding ElementName=comboBoxPalette,Path=Visibility}" 
                               Content="_Palette" />
                        <ComboBox x:Name="comboBoxPalette" SelectedIndex="0"/>
                    </StackPanel>

                    <Button x:Name="buttonTheme" Content="🌙" Grid.Column="1" Width="32" Click="Button_Click" />
                </Grid>
            </Border>

            <Border Grid.Row="2" Padding="8" >
                <ContentControl x:Name="sampleContent" Foreground="{DynamicResource ForegroundColor}" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Stretch" />
            </Border>

        </Grid>
    </Grid>
</Window>
