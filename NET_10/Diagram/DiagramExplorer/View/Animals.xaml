<UserControl x:Class="DiagramExplorer.View.Animals"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:data="clr-namespace:DiagramExplorer.Data"           
             xmlns:c1="http://schemas.componentone.com/winfx/2006/xaml"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <DataTemplate x:Key="NodeTemplate">
            <Border BorderBrush="DarkBlue" BorderThickness="1" CornerRadius="5">
                <StackPanel Margin="8">
                    <TextBlock Text="{Binding}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Image}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <Expander Header="{Binding}">
                        <Expander.Style>
                            <Style TargetType="Expander">
                                <Setter Property="Visibility" Value="Visible"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Image}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Expander.Style>
                        <StackPanel>
                            <TextBlock Text="{Binding NameScientific}" FontStyle="Italic" />
                            <Image Source="{Binding Image}"/>
                        </StackPanel>
                    </Expander>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    <Grid>
        <c1:FlexDiagram x:Name="diagram" 
                        NodeTemplate="{StaticResource NodeTemplate}"
                        Binding = "Phylum,Class,Order,Name" 
                        LegendPosition="None" ScaleMode="ScaleToFit" >
            <c1:FlexDiagram.Resources>
                <Style TargetType="c1:Node">
                    <Setter Property="Shape" Value="RoundedRectangle" />
                    <Setter Property="Title" Value="{Binding Name}" />
                    <Setter Property="TitleImage" Value="{Binding Image}" />
                    <Setter Property="TitleDirection" Value="Vertical" />
                </Style>
            </c1:FlexDiagram.Resources>
        </c1:FlexDiagram>
        <CheckBox x:Name="checkBoxTemplate" HorizontalAlignment="Left" VerticalAlignment="Top" 
                  Width="120" Height="20" Content="Use Node Template" Checked="CheckBox_Checked" Unchecked="CheckBox_Checked" />
    </Grid>
</UserControl>
