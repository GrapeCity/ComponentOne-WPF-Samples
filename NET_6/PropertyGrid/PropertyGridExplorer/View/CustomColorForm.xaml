<UserControl x:Class="PropertyGridExplorer.CustomColorForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:c1="http://schemas.componentone.com/winfx/2006/xaml"
             xmlns:properties="clr-namespace:PropertyGridExplorer.Properties"
             mc:Ignorable="d" 
             d:DesignHeight="219" d:DesignWidth="233">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <c1:C1SpectrumColorPicker x:Name="spectrum" />
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="1" Margin="4">
            <c1:C1Button x:Name="btnOK" Content="OK" Margin="0, 0, 5, 0"/>
            <c1:C1Button x:Name="btnCancel" Content="{x:Static properties:Resources.Cancel}" Margin="5, 0, 0, 0"/>
        </StackPanel>
    </Grid>
</UserControl>
